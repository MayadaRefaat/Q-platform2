@model mr_bahaa.Models.studentquestions

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="container mb-4">
        <div class="row justify-content-center">
            <div class="col-sm-12 col-md-8 col-lg-6  mx-auto justify-content-center">
                <div class="card  my-5 shadow-lg p-3 mb-5 bg-white rounded justify-content-center">
                    <div class="justify-content-center ">
                        <h4 class="text-center card-header">Are you sure you want to delete this student question ?</h4>
                    </div>

                    <div>

                        <hr />

                            <dl class="dl-horizontal">
                                <dt>
                                    @Html.DisplayNameFor(model => model.studentquestion)
                                </dt>

                                <dd>
                                    @Html.DisplayFor(model => model.studentquestion)
                                </dd>

                                <dt>
                                    @Html.DisplayNameFor(model => model.teacherreplay)
                                </dt>

                                <dd>
                                    @Html.DisplayFor(model => model.teacherreplay)
                                </dd>

                               

                            </dl>

                            @using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()

                                <div class="form-actions no-color">
                                    <input type="submit" value="Delete" class="btn danger" /> |
                                    @Html.ActionLink("Back to List", "Index",null,htmlAttributes:new { @class = "btn info" })
                                </div>
                            }
                        </div>
                    </div>
</div>
</div>
</div>
